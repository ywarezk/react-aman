/**
 * How we declare variable in ES6
 */

var hello;
hello = 'world';
hello = 10;

let foo = true;
let bar;
bar = 'hello';

const myArray = [];
myArray.push(1);

function hello(stamBool) {
    if (stamBool) {
        let msg = '';
    }
    console.log(msg);
}
